{
  "name": "set1-implementation",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsc && node build",
    "deploy": "npx tsc && node build deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "web3": "^1.8.1"
  }
}
